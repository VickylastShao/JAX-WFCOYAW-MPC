# 主线推进结算：单一收益累积候选与动态对照资格

2026-09-15。本轮按 `audit/plans/re_mainline_accumulation_20260915/PLAN.md` 完成候选实现、CPU 契约检查、有限 FLORIDyn 资格评估和稿件整合。FLORIDyn 到达科学停止条件；OWN-TAIL-200-v1 尚无 LES 数值资格或闭环收益结果。本轮结果支持下一步的明确取舍，尚未建立新的控制收益贡献。

## 1. 论文主线与当前决定

论文继续回答：如何在偏航执行约束和在线时间预算内，直接使用可微 LES 优化控制，以及这种计算能力在什么信息条件下转化为实际净能量。完整控制价值主张仍要求因果预测下，整段配对净能量同时优于 no-yaw 和外部 B1P*(F)，并通过时限、执行、数值和失败端点。

已有两根八状态诊断在 480 s 呈五项正局部价值、三项排序反转。这说明局部优化值、预测信息和滚动执行之间仍有缺口；它没有证明终端延续假设是唯一原因，也没有证明更换该假设必然提高整程收益。因此本轮只冻结一个可以被否证的候选。

FLORIDyn 本轮单尾流及背景输运均未通过预定条件。停止本适配版本的扩张参数搜索及下游比较，把后续计算优先用于纯 LES-MPC 的直接控制价值检验。资格失败只限定本次实现和工况，不用于宣称 LES 优于动态模型，也不构成 LES 的独立物理验证。

## 2. OWN-TAIL-200-v1：已经实现什么

实现见 `candidate.py`，定义见 `CANDIDATE.json`。正常发令时，先模拟 180 s 执行延迟；新优化的九维目标作用于预测前 200 s，后 280 s 接续本控制器自身暖启动投影所得的固定尾目标。只冻结尾目标，梯度继续穿过完整 480 s 的 LES 状态传播。这个尾目标是固定的未来策略近似，不是已知的下一次优化结果，也不会被强制发送给机组。

保持原四次 Adam 更新、五次值梯度评价、因果 past-20s 均值预测、角度 ±30°、速率 0.3°/s、30 kW 运动成本和 90° 计划总变差限额。首次目标用固定 24 次二分投影；边界尾目标向内收缩总计 10⁻⁴°，确保数值可行起点。最后一次预定发令使用原 480 s 恒目标预测，实际末次命令保持至 2280 s。控制器命令形成路径不读取 B1P*(F)。

CPU 独立代数检查的 2,048 个随机样本通过：最大计划总变差 89.9999999996877°，相对独立分段线性标量投影参考的最大差 2.354722859×10⁻⁶°；尾目标固定、幂等性、末次发令及形状检查通过。**这些只是 NumPy 代数和源码接口检查；JAX/LES 前向、梯度、同步在线时间、闭环净收益均未运行。** 外层执行程序仍须正确传入末次发令标志和原暖启动更新。

固定返回可能增加运动成本、削弱必要持续偏航，或与真实后续决策不符。该候选不提供稳定性、递归可行性或能量单调保证。

## 3. FLORIDyn：接口合格，科学条件未通过

固定 Julia 1.11.9、FLORIDyn.jl 0.5.6、上游提交 `df494099a72109534ce24657cb2fc4893c4b35d7`，沿用既有 C4 的 κ/CT 和一个已暴露根 OC-73090801 的 T1/T2 数据。没有新增 LES、前驱或物理参数候选。全部 287 个上游文件哈希保持原值。

被动观察器以独立状态槽避免原生 grid-point 与已有 OP 索引共用，观测点不施力；原生推进过程和真实机组状态保持一致。零力极限误差为 0，与原生测量数组最大差 4.656612873×10⁻¹⁰。5D/10D 各以 1,080 个保留点重建原 LES 体积权重；FP32/FP64 权重差有记录，未宣称恢复原 GPU 数组的逐位副本。

模型背景速度来自同一组输运 OP 去除 Gaussian 尾流折减后的量，**不是另行积分的无机组流场**。LES 的真实 no-turbine 分支只作离线参考。使用固定 180–660 s 区间内 24 个 20 s 窗口，无滞后拟合或时间对齐调参；场景和窗口不当作独立根。

| 场景/距离 | 模型平均缺损 | LES 平均缺损 | 平均缺损绝对偏差 | 窗口缺损 MAE | 背景速度 NMAE |
|---|---:|---:|---:|---:|---:|
| T1 / 5D | 0.30476 | 0.23070 | 0.07405 | 0.08777 | 7.77% |
| T1 / 10D | 0.16249 | 0.12618 | 0.03630 | 0.05577 | 11.81% |
| T2 / 5D | 0.25239 | 0.21541 | 0.03698 | 0.09262 | 6.94% |
| T2 / 10D | 0.15396 | 0.12126 | 0.03270 | 0.07207 | 6.78% |

四项均超过原单尾流门槛：平均缺损绝对偏差 ≤0.02、窗口 MAE ≤0.03。四项背景 NMAE 也均超过全量结果产生前冻结的 5% 标定入口条件，因此不能把当前误差单独归因于尾流恢复参数。决定为 `stop_no_isolated_recovery_basis`，没有执行 k_b 网格、九机复评、剩余 CAL/QUAL 或动态模型闭环。

独立标准库复算核对 24 个指标，最大差 2.220446049×10⁻¹⁶，重现同一停止决定。具体数据与门槛见 `jobs/B3_BASELINE_SCORE/RESULT.json`、`METRICS.csv`、`BINS.csv` 和 `SCORE_PROTOCOL.md`。此前 C4 功率资格 NO_GO 保持，不由本轮接口通过覆盖。

## 4. 耗时、失败和证据保留

全部 14 次科学进程尝试终止，其中 8 次退出码 0、6 次技术失败退出码 1。每次启动前都有 `outcomes_present=false` 的 alignment、通过的 mainline adjudication、源/输入清单和运行预算。日志和退出记录完整；2,717 个输入清单条目验证通过，其中 19 个已变动路径条目通过保留的原版本哈希追溯。

累计进程墙钟时间 51.008600791 s，包含失败和并行 T1/T2 两个进程各自的时间，不等于 CPU 利用时间或整个会话耗时。T1/T2 并行运行，单进程各约 10.84/10.69 s；没有新增 GPU 作业、LES 分支或前驱。本轮最后一个科学进程结束于北京时间 2026-09-15 19:05:25。当前本轮活动科学作业为 0。

技术失败包括投影幂等性、边界标量参考、地面权重假设、Julia 解析及输出容器类型。均保留原源码和失败日志。B1 最后一轮输出类型修复超出最初重试次数约定，已在 `TECHNICAL_FIX.md` 单独披露；未放宽科学门槛、增加物理候选或突破 1,200 s 总预算。科学条件失败后没有重试或调参。

## 5. 稿件和复核材料

直接编辑原 DOCX：正文只将已有动态模型说明的引用扩展为 S24/S27；SI 新增 S27 一页，纳入既有物理锚点结果及本轮被动观测、单尾流与背景误差结算。原文字、公式、表格和媒体经结构比较保留；未经 LES 检验的候选未写成正文结果。

Microsoft Word 导出的当前正文为 29 页、9 图、3 表；SI 为 51 页、10 图、51 表。改变的正文第 25 页及 SI 第 51 页已全尺寸检查，整份文档以总览图检查分页。规范 DOCX 导出前后哈希一致，正式 PDF 预览已同步。DOCX 继续是唯一可编辑主稿，后续 Word 手工修改后应重新导出预览。

`paper/08_manuscript/supplements/Source_Data_dynamic_comparator_20260915.zip` 为 10,356,632 字节（约 10.36 MB），SHA-256 `b55669d1ddfd577c8af2aa9acde2d32f7df1dede753be0b25712b378385fd83c`。包含 455 个有哈希的文件及清单；以空临时目录、隔离 Python 和标准库脚本验证全部哈希及 24 项计分。它支持固定动作输出的轻量计分复核，不是完整 LES/闭环重跑包。B5 验证回执单独保存，以免修改已验证 ZIP。

最新作者审阅包：`paper/09_journal_targets/renewable-energy/Renewable_Energy_Mainline_20260915.zip`。旧包原字节保留；旧审阅记录在新包内标为历史，不能用其中 NEEDS_ENV 或旧页数解释当前状态。作者事实、访问安排与最终批准仍待补齐；本轮没有实际投稿或公开数据。

## 6. 下一步：候选 G0，而非扩大实验矩阵

`MINIMAL_COMPARISON.json` 和 `WORK_PACKAGES.csv` 已冻结下一阶段顺序：先做 G0 物理资格，再考虑根 02 的 T1/T2 全程开发比较，通过后才进入根 03。每个根两场景比较原 K1/I4、OWN-TAIL、no-yaw、B1P*(F)；已有三臂只在全部身份和账户匹配时复用。两根均已暴露，不是独立确认。

G0 尚待补齐实际远程状态载荷复核、可执行前向/梯度与闭环适配程序、GPU 分配、实测计时和硬预算。原归档元数据已绑定路径及 SHA，但本轮没有现场重查远程载荷。资格矩阵为两根、两场景、400/1200 s 共八状态；检验同目标 2400 步与 1000+1400 步全状态/功率/偏航一致性、原有限差分梯度规程及 ≤180 s 同步在线时限，JIT 单列。

闭环前用 G0 复算确定数值分辨率并前瞻冻结；根 02 合并净能量相对三臂都超过该分辨率且可靠性通过才进入根 03，仍完整报告逐场景失败。两根逐根和合并均优于双参照、并合并优于原 MPC 后，才制定独立确认方案。当前没有候选 GPU/闭环结果，也没有可靠 GPU 完成时间估计；实际启动前按先导计时汇报结束时间。
