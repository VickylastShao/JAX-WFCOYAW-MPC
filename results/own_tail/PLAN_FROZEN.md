# OWN-TAIL G1/G2 因果闭环配对比较：执行冻结方案

2026-09-16，任何本阶段新闭环结果出现前冻结。沿用原PLAN.md的候选、比较臂、窗口、净收益定义、数值带和顺序，不据后续结果调整。

## 已满足的前置

R1六状态552文件独立审计通过：18/18原生FP64排方向差分、54/54跨精度对照和六项原始图混合JVP通过。结合原O2与O3证据，另立RELEASE.json，覆盖八个固定开发状态及12次完整FP32控制器计时，最大160.5792618067935秒。同轨迹净能量差最大0 kWh。RELEASE仅是本开发比较的准入证据，不是工程发布或独立确认。

跨精度通过使用原绝对3e-5 MW/°或相对5%容差。SC-74121203/T2/1200s第六分量FP32为6.28634e-6、FP64为-3.65313e-6 MW/°，绝对差9.93947e-6，按绝对容差通过；保留符号差异及158.11%相对差，不推断其对优化轨迹无影响。原宽尺度FP32差分失败、原严格机器签名失败、新R0/R1判据及局部资格范围分开记录。不新增OWN-TAIL多尺度Taylor结论。

## 固定研究问题与控制器

OWN-TAIL-200-v1能否在因果历史均值预测下改善完整新闭环轨迹的净能量，同时超过无偏航、外部B1P*(F)和原K1？候选SHA-256固定为025f64972b92ac350b6f4f226f52432ab12e0d712eecbe7bc73579fa67c1e109。

九个优化偏航目标，4次Adam更新、5次值梯度，步长1°；前200s执行目标，后280s使用自身暖启动投影后的尾目标；最后一轮恒目标480s。尾目标停止梯度，状态传播保持完整求导。±30°、0.3°/s、计划总行程90°、30 kW运动耗电、180s命令延迟均保持原定义。在线FP32/highest，原生FP64仅作离线资格来源。

每个场景从与历史配对臂完全相同的t=0完整状态和入口归档开始，初始偏航、活动目标、暖启动均为0。新候选独立演化后续状态；不读取原K1的后续状态或目标。B1P*(F)仅参与离线评分，绝不进入名义控制路径或回退。

## 完整执行与验收

0、200、…、1600s共九次发令，预测只使用过去100个平面（20s）均值，恒持3300步覆盖180s延迟及480s预测。真实执行0–2280s，保存11400×9功率和偏航轨迹。完整在线时间包含预测数据构造/传输、延迟状态预测、优化、约束检查与命令形成，九次均须<=180s；JIT单列。失败保持活动目标，保留全部分母和回退；不以短窗口替代失败轨迹。

19个真实执行段均须有限、CFL<1、散度RMS<0.1，偏航幅度和速率满足原容差。完整性、计时、约束、有限性、物理及无回退均合格才判可靠。

独立评分按dt=0.2s，从全部功率与偏航运动重建毛能量、运动耗电、净能量及行程，并逐步重建180s命令延迟。核对九次预测时间边界及保存平面的字节/形状哈希。四条既有完整轨迹对该时序审计代码的验证通过，错移一个时间步的反例全部被拒绝。

## 比较与决策

原K1、B1P*(F)及无偏航参考来自已独立审计的固定四工况32项文件；每次发射前重新核验字节哈希、相同完整状态、入口及共享求解器来源。历史结果当前只属已暴露开发参考。

每场景每参照的数值带固定为：

`max(1e-6 + 1e-5 * max(1, abs(candidate/reference gross/net kWh)), 10 * qualification_max_same_trace_abs_kwh * (2280/480))`。

这里qualification_max_same_trace_abs_kwh=0，完整证据绑定RELEASE。根内带为T1、T2两带之和。此带是工程数值分辨率，不是置信区间。完整报告各场景、根内合并与可用的两根合并结果，所有负值保留。

G1先运行SC-74121202的T1/T2，两场景可靠且合并净能量超过K1、无偏航、B1P*(F)各自数值带，才进入G2。否则停止G2，不修改候选或增加场景。

G2为SC-74121203的T1/T2。两根各自及两根合并均优于无偏航和B1P*(F)，且合并优于K1，才另制新独立确认方案；不自动发射确认，不恢复原N=22。两根均已暴露，G1/G2不称为独立确认或现场可部署验证。

## 资源、计时和停止

使用当前经授权且现场空闲的GPU1/3，各一项并行。每作业2100卡秒硬上限，内层2070秒、外层2085秒加10秒终止；G1合计4200，G1+G2总8400卡秒。每次发射前重新核验UUID、无驻留计算、空闲显存>=120000 MiB、固定镜像和所有源/输入哈希，逐作业前瞻主线门禁通过后方可启动。

实测12次完整控制调用158.79–160.58s；九次按最大值约1445.21s。正常/末次工厂已测JIT约44.70/27.69s，另计plant编译、真实推进、I/O和诊断，预计每波27–32分钟，硬上限35分钟。每波启动前报告实际预计结束时间，按实时进度更新。

任何运行或科研失败均保留，禁止自动重试、换点、改阈值或挑选窗口。作业通过身份核验后才可停止；有限任务核对真实Docker退出、结果、预算和归档。G1独立评分前不发射G2。本轮不修改原始输入、前驱或旧分数。
